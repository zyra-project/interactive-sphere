services:
  app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      # Mount source and public for live editing without rebuild
      - ./src:/app/src
      - ./public:/app/public
      - ./tokens:/app/tokens
      - ./locales:/app/locales
      - ./scripts:/app/scripts
      - ./vite.config.ts:/app/vite.config.ts
      - ./tsconfig.json:/app/tsconfig.json
    environment:
      - CHOKIDAR_USEPOLLING=true  # Reliable file watching in Docker
    # Regenerate tokens on start so output lands in bind-mounted src/.
    # Locales (and privacy-page) are handled by the `predev` script hook
    # in package.json, so we don't re-run them here.
    command: sh -c "npm run tokens && npm run dev -- --host 0.0.0.0"